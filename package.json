{
  "name": "monitoring-listrik-monorepo",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "description": "Monorepo for Monitoring Listrik project",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm install --include=dev --no-package-lock && npm run build -w frontend",
    "start": "npm run start -w backend",
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "install:all": "npm install",
    "backend": "npm run dev -w backend",
    "frontend": "npm run dev -w frontend",
    "simulator": "npm run simulator -w backend",
    "dev:full": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\" \"npm run simulator -w backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}